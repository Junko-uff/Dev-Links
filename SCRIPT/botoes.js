function toggleMode() {
   const html = document.documentElement
   html.classList.toggle("light")

    const img = document.quer

   if(body.classListList.contains('light')) {
    html.classList.remove('light')
    img.set
   } else {
    html.classList.add('light')
   }
}